import React from 'react';

/** A context having the function to set the state of the menu to be closed */
const CurrentMenuStateContext = React.createContext();
export default CurrentMenuStateContext;
