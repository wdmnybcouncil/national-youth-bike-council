'use strict';

/**
 * shareable-links-page service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::shareable-links-page.shareable-links-page');
