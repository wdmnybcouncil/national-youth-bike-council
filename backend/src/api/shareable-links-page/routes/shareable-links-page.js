'use strict';

/**
 * shareable-links-page router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::shareable-links-page.shareable-links-page');
