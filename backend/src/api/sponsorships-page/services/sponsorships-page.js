'use strict';

/**
 * sponsorships-page service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::sponsorships-page.sponsorships-page');
